# Original Design Backup

Snapshot of the site taken before the furnace design system redesign.

To revert, copy these files back into the project root (overwriting the current `index.html`, `styles.css`, `script.js`, and the `machining/` and `services/` directories).
